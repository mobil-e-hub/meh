{
  "name": "meh",
  "version": "1.0.0",
  "description": "mobil-e-Hub: Intelligent Drone Logistics Network",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sim": "nodemon sim/src/server.js",
    "viz": "vue serve viz/src/app.js",
    "opt": "export FLASK_APP=opt/src/server.py && flask run",
    "all": "npm-run-all -p sim viz opt"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  },
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "rules": {
      "no-undef": "off",
      "no-unused-vars": "off"
    }
  }
}
