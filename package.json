{
  "name": "meh",
  "version": "1.0.0",
  "description": "mobil-e-Hub: Intelligent Drone Logistics Network",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sim": "nodemon sim/src/server.js",
    "viz-wss": "nodemon viz/wss/src/server.js",
    "viz-vue": "vue serve viz/vue/src/app.js",
    "opt": "export FLASK_APP=opt/src/server.py && export FLASK_ENV=development && flask run -p 3001",
    "optwindows": "set FLASK_APP=opt/src/server.py && set FLASK_ENV=development && python -m flask run -p 3001",
    "all": "npm-run-all -p sim viz-wss viz-vue opt"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  },
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "rules": {
      "no-undef": "off",
      "no-unused-vars": "off"
    }
  }
}
