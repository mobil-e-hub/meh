{
  "name": "meh",
  "version": "1.0.0",
  "description": "mobil-e-Hub: Intelligent Drone Logistics Network",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "npm-run-all -s setup:*",
    "start": "npm-run-all -p start:*",
    "setup:sim": "npm run setup --prefix sim",
    "setup:opt": "npm run setup --prefix opt",
    "setup:viz": "npm run setup --prefix viz/wss",
    "setup:vue": "npm run setup --prefix viz/vue",
    "start:sim": "npm run start --prefix sim",
    "start:opt": "npm run start --prefix opt",
    "start:viz": "npm run start --prefix viz/wss",
    "start:vue": "npm run start --prefix viz/vue"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  },
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "rules": {
      "no-undef": "off",
      "no-unused-vars": "off"
    }
  }
}
