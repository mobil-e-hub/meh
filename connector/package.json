{
  "name": "mqtt",
  "version": "1.0.0",
  "description": "This module simply connects Azure EventGrid and MQTT broker by forwarding messages to the other component.",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup:mac": "npm install",
    "start:mac": "nodemon src/server.js",
    "setup:windows": "npm install",
    "start:windows": "title connector && nodemon src/server.js"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-json-validator-middleware": "^2.2.1",
    "lodash": "^4.17.21",
    "morgan": "^1.10.0",
    "mqtt": "^4.2.8",
    "mqtt-match": "^2.0.3",
    "axios": "0.25.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.9"
  },
  "author": "",
  "license": "ISC"
}
